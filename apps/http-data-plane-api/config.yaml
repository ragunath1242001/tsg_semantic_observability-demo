db:
  type: "sqlite"
  database: "test.db"

auth:
  enabled: false
  # authorizationURL: https://localhost/login/oauth/authorize
  # tokenURL: https://localhost/api/login/oauth/access_token
  # introspectionURL: https://localhost/api/login/oauth/introspect
  # callbackURL: http://localhost:5173/api/auth/callback
  # clientId:
  # clientSecret:
  # clientUsername:
  # clientPassword:
  # rolePath: "$.roles[*].name"
  # redirectURL: http://localhost:5173/

controlPlane:
  dataPlaneEndpoint: http://localhost:3000/api/data-plane
  managementEndpoint: http://localhost:3000/api/management
  controlEndpoint: http://localhost:3000

runtime:
  color: "#3B8BF6"

dataset:
  title: Product Catalog Data App
  baseSemanticModelRef: https://zerow.semantic-treehouse.nl/ontology/Ontology_1b9b1d8d-8a2c-4012-8883-f0bfff497205
  currentVersion: 0.1.0
  versions:
    - version: 0.1.0
      distributions:
        - openApiSpecRef: https://app.productcatalog.zerow.dataspac.es/api-json
          backendUrl: https://app.productcatalog.zerow.dataspac.es
  policy:
    type: "default"
