apiVersion: v1
kind: Service
metadata:
  name: {{ template "wallet.fullname" . }}
  labels: {{ include "wallet.labels" $ | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ template "wallet.fullname" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
