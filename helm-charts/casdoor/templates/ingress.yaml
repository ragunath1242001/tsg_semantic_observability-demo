{{- if .Values.ingress.enabled }}
{{- with .Values.ingress }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "casdoor.fullname" $ }}-ingress
  labels: {{ include "casdoor.labels" $ | nindent 4 }}
  annotations:
    {{- if .clusterIssuer }}
    cert-manager.io/cluster-issuer: {{ .clusterIssuer }}
    {{- end }}
    {{- with .annotations }}
    {{- tpl (toYaml . ) $ | nindent 4 }}
    {{- end }}
    {{- if .rewriteTarget}}
    nginx.ingress.kubernetes.io/rewrite-target: {{ .rewriteTarget }}
    {{- end }}
spec:
  ingressClassName: {{ .class | default "nginx" }} 
  tls:
  - hosts:
    - {{ required "A hostname under the key 'host' is required to enable ingresses" $.Values.host }}
    secretName: {{ $.Values.host }}-tls-secret
  rules:
  - host: {{ $.Values.host }}
    http:
      paths:
      - backend:
          service:
            name: {{ template "casdoor.fullname" $ }}
            port: 
              number: {{ $.Values.config.httpport | default 8000 }}
        path: {{ .path | default "/" }} 
        pathType: Prefix
{{- end }}
{{- end }}